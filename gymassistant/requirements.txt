# Зависимости бота.
#
# Раньше этот файл был общим на все сервисы, и Dockerfile бота ставил его целиком —
# в образе бота лежал PyTorch, который боту не нужен ни для чего. Теперь у каждого
# сервиса свой список:
#   requirements.txt           — бот
#   requirements-miniapp.txt   — Mini App
#   press_api/requirements.txt — LSTM-сервис (вот там PyTorch и живёт)
#
# Общий у них только слой данных (database/) — его зависимости продублированы.

# Bot
aiogram==3.13.1
aiohttp==3.10.10
aiohttp_socks==0.11.0
python-dotenv==1.0.1
pydantic==2.8.2
magic-filter==1.0.12
pillow==11.0.0

# Database
SQLAlchemy==2.0.36
asyncpg==0.29.0
aiosqlite==0.20.0
alembic==1.13.3
