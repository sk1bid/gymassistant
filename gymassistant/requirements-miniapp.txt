# Зависимости Mini App.
#
# Ни aiogram, ни HTTP-клиента: веб-сервис не ходит наружу вообще. Подпись initData
# проверяется локально через hmac из стандартной библиотеки, а прогноз веса убран
# вместе с press-api.

# API
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.8.2
python-dotenv==1.0.1

# Database — тот же слой данных, что у бота
SQLAlchemy==2.0.36
asyncpg==0.29.0
aiosqlite==0.20.0
