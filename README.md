# GYM.assistant 👊🏻💪

![Python](https://img.shields.io/badge/python-3.12-blue.svg)
![Aiogram](https://img.shields.io/badge/aiogram-3.x-red.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-latest-009688.svg)
![Kubernetes](https://img.shields.io/badge/Kubernetes-Production-blue.svg)
![Docker](https://img.shields.io/badge/Docker-Ready-2496ED.svg)

**GYM.assistant** — это продвинутый Telegram-бот для планирования тренировок и отслеживания прогресса с интегрированной нейросетью для прогнозирования нагрузок.

[Попробовать бота](https://t.me/gymassit_bot?start=readme)

---

## 🌟 Основные возможности

- **Управление тренировками**: Создание собственных программ, выбор упражнений из базы или добавление своих.
- **Умный помощник (AI)**: Интегрированная LSTM-нейросеть анализирует последние 5 подходов в жиме лежа и рекомендует оптимальный вес для следующего сета.
- **Статистика и история**: Визуализация прогресса, хранение истории каждой сессии и автоматическое отслеживание личных рекордов.
- **Админ-панель**: Управление баннерами и базой упражнений прямо через интерфейс бота.
- **Отказоустойчивость**: Микросервисная архитектура с разделением логики бота и вычислительного API нейросети.

---

## 🛠 Технологический стек

- **Backend**: Python 3.12, Aiogram 3.x (Bot), FastAPI (Neiro API).
- **Database**: PostgreSQL + SQLAlchemy (AsyncPG), Alembic для миграций.
- **AI/ML**: PyTorch (модель LSTM), Scikit-learn (препроцессинг данных).
- **DevOps**: Docker, Docker Compose, Kubernetes (K8s).
- **CI/CD**: GitHub Actions (автоматическая сборка образов и деплой на сервер).

---

## 🏗 Архитектура системы

```mermaid
graph TD
    User((Пользователь)) <--> TG[Telegram API]
    TG <--> Bot[Bot Service (Aiogram)]
    Bot <--> DB[(PostgreSQL)]
    Bot <--> API[Neiro API (FastAPI)]
    API <--> Model[LSTM Model (.pt)]
    
    subgraph "Infrastructure"
        Bot
        API
        DB
    end
```

---

## 🧠 Нейросеть (LSTM)

В проекте используется рекуррентная нейронная сеть (Long Short-Term Memory) для анализа временных рядов тренировочных данных. Модель обучалась на реальных данных атлетов и учитывает:
1. Вес предыдущих подходов.
2. Количество выполненных повторений.
3. Тоннаж и интенсивность.
4. Фактор утомляемости (порядок подхода в сессии).

---

## 🚀 Быстрый запуск

### Локально через Docker Compose
Самый простой способ запустить весь стек (Бот + API + БД) одной командой:

1. Склонируйте репозиторий.
2. Создайте `.env` на основе `.env.example`.
3. Запустите:
   ```bash
   docker-compose up -d --build
   ```

### Деплой в Kubernetes
Проект полностью готов к деплою в K8s кластер. Манифесты находятся в папке `k8s/`.
- Автоматический деплой настроен через GitHub Actions.
- Используются `Secrets` для безопасного хранения токенов.

---

## 📂 Структура проекта

- `gymassistant/` — основной исходный код.
  - `handlers/` — логика взаимодействия с пользователем.
  - `services/` — интеграция с API и внешними системами.
  - `database/` — модели данных и ORM запросы.
  - `press_api/` — микросервис нейросети (FastAPI + PyTorch).
- `k8s/` — конфигурационные файлы для Kubernetes.
- `docker-compose.yml` — оркестрация для локальной разработки.
- `.github/workflows/` — автоматизация CI/CD.

---

Разработано как демонстрационный проект для портфолио. 🏋️‍♂️
